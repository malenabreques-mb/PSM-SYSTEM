<Window x:Class="PSMSystem.Views.AvancesOrdenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Titulo}"
        Width="480" Height="560"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock FontSize="16" FontWeight="Bold">
                <Run Text="Orden #"/><Run Text="{Binding Orden.IdOrdenTrabajo, Mode=OneWay}"/>
            </TextBlock>
            <TextBlock Text="{Binding Orden.Vehiculo.DescripcionCombo}" Foreground="Gray"/>
            <TextBlock Text="{Binding Orden.MotivoIngreso}" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Avances}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="90"/>
                <DataGridTextColumn Header="Etapa" Binding="{Binding Etapa}" Width="130"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Margin="0,12,0,0"
                    Visibility="{Binding MuestraFormularioAlta, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Nuevo avance" FontWeight="Bold" Margin="0,0,0,6"/>

            <TextBlock Text="Etapa"/>
            <ComboBox ItemsSource="{Binding Etapas}" SelectedItem="{Binding EtapaSeleccionada}" Margin="0,2,0,10"/>

            <TextBlock Text="Descripción"/>
            <TextBox Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap" AcceptsReturn="True" Height="50" Margin="0,2,0,10"/>

            <TextBlock Text="Fecha"/>
            <DatePicker SelectedDate="{Binding Fecha}" Margin="0,2,0,10"/>

            <TextBlock Text="{Binding MensajeError}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,6"/>

            <Button Content="+ Agregar avance" Command="{Binding AgregarAvanceCommand}"
                    HorizontalAlignment="Left" Padding="10,4"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cerrar" Command="{Binding CerrarCommand}" Width="90"/>
        </StackPanel>
    </Grid>
</Window>